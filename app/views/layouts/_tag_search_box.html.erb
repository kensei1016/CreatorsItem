<% ActsAsTaggableOn::Tag.most_used.each do |tag| %>
  <%= link_to tag.name, work_rooms_path(tag_name: tag.name), class:"page-link text-dark d-inline-block rounded-pill tag_search_box__tag_btn" %>
<% end %>