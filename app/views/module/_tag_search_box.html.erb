<% ActsAsTaggableOn::Tag.most_used.each do |tag| %>
  <%= link_to tag.name, work_rooms_path(tag_name: tag), class:"page-link text-dark d-inline-block rounded-pill work_rooms_recomend__tag_btn" %>
<% end %>